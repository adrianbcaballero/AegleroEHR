# Loaded automatically by Next.js when NODE_ENV=production (i.e. `pnpm build`).
# Empty value = fetch calls are relative paths (/api/...). CloudFront routes
# them to the ALB on the same origin, so no CORS, no separate API hostname.
NEXT_PUBLIC_API_BASE_URL=
